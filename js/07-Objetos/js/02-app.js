//Como acceder a los valores de un objecto


const productos = {
    nombre:"Monitor",
    pecio:3000,
    disponible:true,
    id:0
}

console.log(`El ${productos.nombre} tiene un valor de $${productos.pecio} USD`);